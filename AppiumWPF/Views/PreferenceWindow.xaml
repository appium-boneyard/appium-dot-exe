<Window
    x:Class="Appium.Views.PreferenceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:Appium.Views"
    Title="PreferenceWindow"
    Height="330"
    Width="500">

    <StackPanel
        Orientation="Vertical">

        <!-- 
        like how the apple appium app looks
        <TabControl
            Height="300">
            <TabItem
                Header="General">
                <views:GeneralPreferencesView />
            </TabItem>
            <TabItem
                Header="Logging">
            </TabItem>
            <TabItem
                Header="Android">
            </TabItem>
        </TabControl>
        -->
        <views:GeneralPreferencesView
            Margin="10"
            DataContext="{Binding GeneralPreferencesVM}" />
    </StackPanel>
</Window>
